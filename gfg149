class Solution:
    def Maximize(self, arr): 
        # Complete the function
        arr.sort()
        sum=0
        for i in range(1,len(arr)):
            sum+=arr[i]*i
        if (sum>10**9+7): return sum %(10**9+7)    
        return sum   
      
